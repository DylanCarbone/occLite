% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summariseMSI.R
\name{modified_summariseMSI}
\alias{modified_summariseMSI}
\title{Summarise and Plot Occupancy Index Trends}
\usage{
modified_summariseMSI(
  label,
  plotType,
  indicator,
  method,
  plot = TRUE,
  writePlot = TRUE,
  minYear,
  maxYear
)
}
\arguments{
\item{label}{Character. A label to use as the plot title and file name if saving plots.}

\item{plotType}{Character. Type of plot to produce. Options are \code{"indicator"} (for time-series index),
\code{"nSpecies"} (number of species through time), or \code{"uncertainty"} (width of interval vs number of species).}

\item{indicator}{A list containing the output of a multi-species indicator analysis,
typically including components like \code{Summary}, \code{st}, \code{lt}, \code{final}, and \code{MetaData}.}

\item{method}{Character. The method used to compute trends, e.g. \code{"lambda"}. Required to enable trend bar plots.}

\item{plot}{Logical. If \code{TRUE}, plots are displayed; if \code{FALSE}, summary data is returned instead.}

\item{writePlot}{Logical. If \code{TRUE} and \code{plotType == "indicator"} with \code{method == "lambda"}, the plot will be saved to disk.}

\item{minYear}{Integer. The minimum year to include in plots.}

\item{maxYear}{Integer. The maximum year to include in plots.}
}
\value{
If \code{plot = TRUE}, returns a ggplot or grid of plots. If \code{plot = FALSE}, returns a list of:
\itemize{
\item \code{out1}: A data.frame of summarised occupancy values by year
\item \code{change}: A table of species trend categories across time periods
\item \code{raw}: A data.frame of raw trend category labels for each species
}
}
\description{
A modified function to summarise and optionally visualise occupancy index results
from multi-species indicators (MSI). Produces either time-series plots of the
index, bar plots of species trend assessments, or plots of index uncertainty
versus number of contributing species. Optionally returns summary data tables instead of plots.
}
\examples{
# Example using precomputed indicator
modified_summariseMSI(
  label = "Ants",
  plotType = "indicator",
  indicator = ants_indicator_result,
  method = "lambda",
  plot = TRUE,
  writePlot = FALSE,
  minYear = 1990,
  maxYear = 2020
)

}
